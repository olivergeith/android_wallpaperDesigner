package de.geithonline.wallpaperdesigner.bitmapdrawer.raster;

public enum RasterPositioning {
	RANDOM, BOOK, BOOK_REVERSE, TOWER, CENTER, INNER, OUTER, TOPMOST, LEFTMOST, BOTTOMMOST, RIGHTMOST;
}
